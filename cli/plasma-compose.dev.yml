services:
  plasma-server:
    container_name: plasma-server
    build: .
    ports:
      - "127.0.0.1:8080:8080"
      - "127.0.0.1:8081:8081"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PLASMA_DEVELOPMENT=true
